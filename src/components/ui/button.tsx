"use client";

import {
  GlassButton,
  type GlassButtonProps,
  glassButtonVariants,
  glassButtonTextVariants,
} from "@/components/interactive/GlassButton";

export type ButtonProps = GlassButtonProps;
export const Button = GlassButton;
export const buttonVariants = glassButtonVariants;
export const buttonTextVariants = glassButtonTextVariants;
export default Button;
